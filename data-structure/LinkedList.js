function LinkedList() {
  function Node(newVal) {
    var val = newVal;
    var next = null;

    return {
      val,
      next
    };
  }

  var length = 0;
  var head = null;
  var tail = null;
}

function Node(newVal) {
  var val = newVal;
  var next = null;

  return {
    next
  };
}
